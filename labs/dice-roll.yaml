- route:
    id: route-1146
    from:
      id: from-1242
      uri: platform-http:/dice-roll
      parameters: {}
      steps:
        - setBody:
            id: setBody-4183
            expression:
              simple:
                expression: "dice-roll: ${random(1,6)}"
