- route:
    id: route-3968
    from:
      id: from-002f
      uri: kamelet:timer-source
      parameters:
        message: hello
      steps:
        - log:
            id: log-ddf5
            message: ${body}
